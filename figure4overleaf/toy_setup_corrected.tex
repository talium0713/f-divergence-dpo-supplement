% =============================================================================
%  Corrected toy-experiment setup — verified against the code on 2026-09-15.
%
%  Replaces App. D.2 (Setup) and App. F.1 (Toy tree MDP) + Fig. 12.
%  The previous text described a DETERMINISTIC COMPLETE TREE (W=3, H=3, |S|=40,
%  leaf-only N(0,1) rewards). The implemented environment is a STOCHASTIC
%  LAYERED (trellis) MDP. Ground truth:
%     python/mdp.py         SN=3, NA=3, DEFAULT_DEPTH=4, DEFAULT_EPS=0.2,
%                           transP(), new_rewards() = Uniform(-0.8, 0.8)
%     python/regularizers.py  DEFAULT_ADIV_A = 0.5
%     python/dpo.py         Bradley-Terry oracle on DISCOUNTED returns, temp 1
%     python/experiments.py calibrate() -> alpha by bisection per (Omega, peak)
%     run manifest          env = {gamma:0.9, eps:0.2, depth:4, SN:3, NA:3,
%                                  reward:"Uniform(-0.8,0.8)"}
%                           config = {n_mdp:100, n_seeds:1, steps:400,
%                                     lr:0.08, batch:16, npairs:6000}
%
%  PREAMBLE REQUIREMENTS:
%     \usepackage{tikz}
%     \usetikzlibrary{arrows.meta}
%     \usepackage{amsmath,amssymb}
%     \usepackage{graphicx}          % for \resizebox
%
%  LAYOUT NOTE: the picture is wide (the legend sits to the right of the trellis).
%  In a two-column paper use \begin{figure*}...\end{figure*} so \resizebox does not
%  shrink the labels; in a one-column paper \begin{figure} as written is fine.
% =============================================================================


% -----------------------------------------------------------------------------
% APP. D.2  —  SETUP
% -----------------------------------------------------------------------------
\subsection{Setup}
\label{app:toy-setup}

\paragraph{Environment.}
Fig.~\ref{fig:toy-mdp} shows the toy environment. It is a \emph{layered} MDP with
$H=4$ decision layers, $S=3$ states per layer and $|\mathcal{A}|=3$ actions, so a policy is a
$4\times3\times3$ table of action distributions and there are $|\mathcal{S}|=12$ state--layer
cells. Transitions depend only on the action and are stochastic: from any state of layer
$\ell$, action $a$ reaches state $a$ of layer $\ell+1$ with probability $1-\epsilon$ and each
of the two remaining states with probability $\epsilon/2$, with $\epsilon=0.2$. Rewards
$r(\ell,s,a)\sim\mathrm{Unif}(-0.8,0.8)$ are drawn i.i.d.\ on \emph{every} (layer, state,
action) triple once per seed and held fixed for that seed; the discount is $\gamma=0.9$. Each
trajectory starts from a uniformly drawn layer-$0$ state and runs for exactly $H$ steps. The
reference policy $\pi_{\mathrm{ref}}$ is uniform on every state. We instantiate seven
regularizers from Tab.~1, namely reverse KL (RKL), $\alpha$-divergence at $\alpha=0.5$,
forward KL (FKL), Jensen--Shannon, squared Hellinger, Pearson $\chi^2$, and squared Euclidean.
Preferences are generated by a Bradley--Terry oracle on \emph{discounted} trajectory returns at
temperature $\beta_{\mathrm{or}}=1$. Training fits tabular logits with the trajectory-DPO loss
($6000$ preference pairs, $400$ steps, batch $16$, learning rate $0.08$); the regularization
weight $\alpha$ is \emph{not} fixed but calibrated per (regularizer, target peak) by bisection,
so that all seven $\Omega$ are compared at matched policy sharpness. Every cell is averaged over
$100$ independently drawn MDPs (one training seed each).

All seven regularizers, including KL, use the same per-step Monte-Carlo budget. In the
\emph{on-policy} regime, $n_{\mathrm{mc}}$ samples are drawn at every non-terminal next-state to
estimate the inner term $\bar\Psi$; in the \emph{off-policy} regime the single logged next action
$a'$ is used instead, so $n_{\mathrm{mc}}$ does not apply. This keeps the per-step compute matched
across $\Omega$ and prevents any sampling-budget advantage from confounding the variance
comparison. For KL, the $n_{\mathrm{mc}}$ draws have no effect ($\bar\Psi_{\mathrm{KL}}\equiv1$
collapses every sample to the same constant, so $n_{\mathrm{mc}}$ cannot change the KL gradient),
but the samples are still drawn.


% -----------------------------------------------------------------------------
% APP. F.1  —  TOY LAYERED MDP  (+ Fig. 12 replacement)
% -----------------------------------------------------------------------------
\subsection{Toy layered MDP}
\label{app:toy-mdp}

The multi-step environment is a layered (trellis) MDP, not a tree. There are $H=4$ decision
layers, each with $S=3$ states, and $|\mathcal{A}|=3$ actions, giving $|\mathcal{S}|=12$
state--layer cells. Transitions are stochastic and depend only on the action: from any state of
layer $\ell$, action $a$ moves to state $a$ of layer $\ell+1$ with probability $1-\epsilon=0.8$,
and to each of the other two states with probability $\epsilon/2=0.1$ (so $\epsilon=0.2$). The
last layer is terminal: its actions collect reward but bootstrap no successor. Per-triple rewards
$r(\ell,s,a)\sim\mathrm{Unif}(-0.8,0.8)$ are sampled once per random seed and held fixed for that
seed, and the discount is $\gamma=0.9$. Trajectories start from a uniformly drawn layer-$0$ state
and run for exactly $H$ steps. The reference policy is uniform,
$\pi_{\mathrm{ref}}(a\mid s)=1/|\mathcal{A}|$ at every state.

\begin{figure}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
  >=Stealth,
  state/.style ={circle, draw=black!55, fill=black!8,  minimum size=7.5mm, inner sep=0pt},
  hl/.style    ={circle, draw=blue!55!black, fill=blue!14, line width=0.9pt,
                 minimum size=7.5mm, inner sep=0pt},
  faint/.style ={draw=black!14, ->, line width=0.4pt},
  main/.style  ={draw=black!80, ->, line width=1.5pt},
  side/.style  ={draw=black!45, ->, line width=0.6pt},
  traj/.style  ={draw=red!75!black, ->, line width=1.7pt},
  lab/.style   ={font=\scriptsize, text=black!60},
  plab/.style  ={font=\scriptsize, text=black!75},
]

% ---- states: 4 decision layers x 3 states ----
\foreach \l [evaluate=\l as \x using 3.1*\l] in {0,1,2,3}{
  \foreach \s [evaluate=\s as \y using -1.7*\s] in {0,1,2}{
    \node[state] (L\l S\s) at (\x, \y) {};
  }
  \node[lab] at (\x, -4.15) {layer $\ell_{\l}$};
}
\node[lab, anchor=east] at (-0.75,  0.0)   {$s=0$};
\node[lab, anchor=east] at (-0.75, -1.7)   {$s=1$};
\node[lab, anchor=east] at (-0.75, -3.4)   {$s=2$};

% ---- faint full connectivity between adjacent layers (the trellis) ----
\foreach \l [evaluate=\l as \nx using int(\l+1)] in {0,1,2}{
  \foreach \s in {0,1,2}{ \foreach \t in {0,1,2}{
      \draw[faint] (L\l S\s) -- (L\nx S\t);
  }}
}

% ---- highlight one state and the epsilon-fan of a single action a=0 ----
\node[hl] at (0,-1.7) {};
\draw[main] (L0S1) -- node[plab, sloped, above, pos=0.56] {$a{=}0:\;1-\epsilon=0.8$} (L1S0);
\draw[side] (L0S1) -- node[lab,  sloped, above, pos=0.62] {$\epsilon/2=0.1$}          (L1S1);
\draw[side] (L0S1) -- node[lab,  sloped, below, pos=0.58] {$\epsilon/2=0.1$}          (L1S2);

% ---- one sampled trajectory ----
\draw[traj] (L0S1) -- (L1S0);
\draw[traj] (L1S0) -- (L2S2);
\draw[traj] (L2S2) -- (L3S1);
\node[font=\small, text=red!75!black] at (10.0,-1.28) {$\tau$};

% ---- side annotations ----
\node[anchor=west, align=left, font=\small] at (10.6,  0.0)
  {$\pi_{\mathrm{ref}}(\cdot\mid s)=\mathrm{uniform}(|\mathcal{A}|)$};
\node[anchor=west, align=left, font=\small] at (10.6, -1.7)
  {stochastic transitions\\[2pt]
   $P(s'=a\mid a)=1-\epsilon$\\[2pt]
   $P(s'\neq a\mid a)=\epsilon/2$ each};
\node[anchor=west, align=left, font=\small] at (10.6, -3.6)
  {$r(\ell,s,a)\sim\mathrm{Unif}(-0.8,0.8)$\\[2pt]
   $\gamma=0.9,\quad \epsilon=0.2$};

\end{tikzpicture}}
\caption{\textbf{Toy layered MDP topology} ($H=4$ layers, $S=3$ states per layer,
$|\mathcal{A}|=3$, $|\mathcal{S}|=12$). Every state of a layer connects to every state of the
next (faint edges); the last layer is terminal. Transitions depend only on the action and are
$\epsilon$-stochastic: the highlighted state shows action $a{=}0$ reaching its target state with
probability $1-\epsilon=0.8$ and each other state with $\epsilon/2=0.1$. Rewards are drawn i.i.d.\
$\mathrm{Unif}(-0.8,0.8)$ on every (layer, state, action) triple, not only at the end of an
episode. The reference policy $\pi_{\mathrm{ref}}$ is uniform. The bold red path shows one
trajectory $\tau$ sampled in trajectory-DPO training, starting from a uniformly drawn layer-$0$
state; preferences are generated by a Bradley--Terry oracle on pairs of such trajectories.}
\label{fig:toy-mdp}
\end{figure}


% =============================================================================
% APP. C — HYPERPARAMETER TABLE
% Replace the two tree rows with the layered parameters. Values are the mdp.py
% defaults (SN, NA, DEFAULT_DEPTH, DEFAULT_EPS) and the run manifests, not prose.
% =============================================================================
%
% DELETE these two rows:
%     $W = 3$ & tree width \\
%     $H = 3$ & tree depth \\
%
% INSERT these instead:

$H = 4$              & decision layers (\texttt{mdp.DEFAULT\_DEPTH})                      \\
$S = 3$              & states per layer (\texttt{mdp.SN})                                 \\
$|\mathcal{A}| = 3$  & actions per state (\texttt{mdp.NA})                                \\
$|\mathcal{S}| = 12$ & state--layer cells ($H \times S$)                                  \\
$\epsilon = 0.2$     & transition noise: $P(s'{=}a\mid a)=1-\epsilon$, else uniform over
                       the other two states (\texttt{mdp.DEFAULT\_EPS})                   \\
$\gamma = 0.9$       & discount                                                           \\
$r \sim \mathrm{Unif}(-0.8, 0.8)$ & reward on every $(\ell, s, a)$, drawn once per seed    \\

% If the table also carries the training row, these are the manifest values:
%     6000 preference pairs, 400 steps, batch 16, learning rate 0.08,
%     100 independently drawn MDPs x 1 training seed,
%     regularization weight calibrated per (Omega, target peak) by bisection.
